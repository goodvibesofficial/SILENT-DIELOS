local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local localPlayer = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- Configuración de Estados
local settings = {
	aimbotEnabled = false,
	espEnabled = false,
	aimKey = Enum.UserInputType.MouseButton2, -- Clic derecho para activar el aimbot
}

-- Almacén para los elementos visuales del ESP
local espCache = {}

--------------------------------------------------------------------------------
-- 1. CREACIÓN DE LA INTERFAZ GRÁFICA (GUI)
--------------------------------------------------------------------------------
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "VibeControlPanel"
screenGui.ResetOnSpawn = false
screenGui.Parent = CoreGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 200, 0, 150)
mainFrame.Position = UDim2.new(0.05, 0, 0.2, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = mainFrame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundTransparency = 1
title.Text = "Control Panel"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 16
title.Font = Enum.Font.GothamBold
title.Parent = mainFrame

-- Botón Aimbot
local aimbotBtn = Instance.new("TextButton")
aimbotBtn.Size = UDim2.new(0.9, 0, 0, 40)
aimbotBtn.Position = UDim2.new(0.05, 0, 0.3, 0)
aimbotBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
aimbotBtn.Text = "Aimbot: OFF"
aimbotBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
aimbotBtn.TextSize = 14
aimbotBtn.Font = Enum.Font.GothamMedium
aimbotBtn.Parent = mainFrame
Instance.new("UICorner", aimbotBtn).CornerRadius = UDim.new(0, 6)

-- Botón ESP
local espBtn = Instance.new("TextButton")
espBtn.Size = UDim2.new(0.9, 0, 0, 40)
espBtn.Position = UDim2.new(0.05, 0, 0.65, 0)
espBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
espBtn.Text = "ESP Esqueleto: OFF"
espBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
espBtn.TextSize = 14
espBtn.Font = Enum.Font.GothamMedium
espBtn.Parent = mainFrame
Instance.new("UICorner", espBtn).CornerRadius = UDim.new(0, 6)

--------------------------------------------------------------------------------
-- 2. LÓGICA DE INTERACCIÓN DE LA GUI
--------------------------------------------------------------------------------
aimbotBtn.MouseButton1Click:Connect(function()
	settings.aimbotEnabled = not settings.aimbotEnabled
	if settings.aimbotEnabled then
		aimbotBtn.Text = "Aimbot: ON"
		aimbotBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
	else
		aimbotBtn.Text = "Aimbot: OFF"
		aimbotBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
	end
end)

espBtn.MouseButton1Click:Connect(function()
	settings.espEnabled = not settings.espEnabled
	if settings.espEnabled then
		espBtn.Text = "ESP Esqueleto: ON"
		espBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
	else
		espBtn.Text = "ESP Esqueleto: OFF"
		espBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
		-- Limpiar ESP al apagar
		for _, data in pairs(espCache) do
			if data.lines then
				for _, line in pairs(data.lines) do
					line:Remove()
				end
			end
		end
		espCache = {}
	end
end)

--------------------------------------------------------------------------------
-- 3. FUNCIONES DE VALIDACIÓN (Ignorar amigos / mismo equipo)
--------------------------------------------------------------------------------
local function isEnemy(player)
	if player == localPlayer then return false end
	
	-- Ignorar si están en el mismo equipo (si tu juego usa Teams)
	if localPlayer.Team and player.Team and localPlayer.Team == player.Team then
		return false
	end
	
	-- Ignorar amigos de Roblox
	local success, isFriend = pcall(function()
		return localPlayer:IsFriendsWith(player.UserId)
	end)
	if success and isFriend then
		return false
	end
	
	return true
end

--------------------------------------------------------------------------------
-- 4. SISTEMA DE AIMBOT (Redirigir a enemigo más cercano)
--------------------------------------------------------------------------------
local function getClosestEnemy()
	local closestTarget = nil
	local shortestDistance = math.huge

	for _, player in ipairs(Players:GetPlayers()) do
		if isEnemy(player) and player.Character then
			local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
			local head = player.Character:FindFirstChild("Head")
			
			if humanoid and humanoid.Health > 0 and head then
				local screenPos, onScreen = camera:WorldToViewportPoint(head.Position)
				if onScreen then
					local mousePos = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
					local distance = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
					
					if distance < shortestDistance then
						shortestDistance = distance
						closestTarget = head
					end
				end
			end
		end
	end

	return closestTarget
end

RunService.RenderStepped:Connect(function()
	if settings.aimbotEnabled then
		-- Si se presiona el botón asignado, ajusta la cámara o dirección hacia el enemigo
		local target = getClosestEnemy()
		if target then
			camera.CFrame = CFrame.new(camera.CFrame.Position, target.Position)
		end
	end

--------------------------------------------------------------------------------
-- 5. SISTEMA ESP ESQUELETO RAINBOW
--------------------------------------------------------------------------------
	if settings.espEnabled then
		-- Color dinámico estilo Rainbow basado en el tiempo
		local rainbowColor = Color3.fromHSV(tick() % 5 / 5, 1, 1)

		-- Definir conexiones de huesos comunes en R15 y R6
		local bonesR15 = {
			{"Head", "UpperTorso"},
			{"UpperTorso", "LowerTorso"},
			{"UpperTorso", "LeftUpperArm"}, {"LeftUpperArm", "LeftLowerArm"}, {"LeftLowerArm", "LeftHand"},
			{"UpperTorso", "RightUpperArm"}, {"RightUpperArm", "RightLowerArm"}, {"RightLowerArm", "RightHand"},
			{"LowerTorso", "LeftUpperLeg"}, {"LeftUpperLeg", "LeftLowerLeg"}, {"LeftLowerLeg", "LeftFoot"},
			{"LowerTorso", "RightUpperLeg"}, {"RightUpperLeg", "RightLowerLeg"}, {"RightLowerLeg", "RightFoot"}
		}

		for _, player in ipairs(Players:GetPlayers()) do
			if isEnemy(player) and player.Character then
				local char = player.Character
				if not espCache[player] then
					espCache[player] = { lines = {} }
				end

				-- Dibujar líneas de esqueleto usando Drawing API (Líneas 2D en pantalla)
				for i, bonePair in ipairs(bonesR15) do
					local p1 = char:FindFirstChild(bonePair[1])
					local p2 = char:FindFirstChild(bonePair[2])

					if p1 and p2 then
						local pos1, onScreen1 = camera:WorldToViewportPoint(p1.Position)
						local pos2, onScreen2 = camera:WorldToViewportPoint(p2.Position)

						if onScreen1 or onScreen2 then
							if not espCache[player].lines[i] then
								local line = Drawing.new("Line")
								line.Thickness = 1.5
								line.Visible = false
								espCache[player].lines[i] = line
							end

							local line = espCache[player].lines[i]
							line.From = Vector2.new(pos1.X, pos1.Y)
							line.To = Vector2.new(pos2.X, pos2.Y)
							line.Color = rainbowColor
							line.Visible = true
						else
							if espCache[player].lines[i] then
								espCache[player].lines[i].Visible = false
							end
						end
					end
				end
			else
				-- Limpiar si el jugador ya no es válido o está muerto
				if espCache[player] and espCache[player].lines then
					for _, line in pairs(espCache[player].lines) do
						line.Visible = false
					end
				end
			end
		end
	end
end)

